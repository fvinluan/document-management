spring:
  application:
    name: document-management
  data:
    mongodb:
      uri: mongodb://localhost:27017/docmanagement
  servlet:
    multipart:
      max-file-size: 100MB
      max-request-size: 100MB

server:
  port: 8080

logging:
  level:
    com.docmanagement: DEBUG
